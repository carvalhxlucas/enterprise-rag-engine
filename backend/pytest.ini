[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    evals: RAG evaluation tests (LLM-as-judge, requires OPENAI_API_KEY)
    unit: Unit tests (no external services)
