[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: fast unit tests, no external services
    evals: RAG evaluation tests, slower and use LLM/API tokens
filterwarnings =
    ignore::DeprecationWarning
